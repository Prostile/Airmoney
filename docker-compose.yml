services:
  vpn-gateway:
    image: ${SING_BOX_IMAGE:-ghcr.io/sagernet/sing-box:v1.13.13}
    container_name: airmoney-vpn
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - ./vpn/sing-box.json:/etc/sing-box/config.json:ro
      - sing-box-data:/var/lib/sing-box
    command: ["run", "--config", "/etc/sing-box/config.json", "--directory", "/var/lib/sing-box"]
    expose:
      - "10808"
    networks:
      - airmoney_net

  container-1:
    container_name: container-1
    build:
      context: .
    restart: unless-stopped
    depends_on:
      vpn-gateway:
        condition: service_started
    env_file:
      - .env
    environment:
      AIRMONEY_BROWSER_PROXY: http://vpn-gateway:10808
      HTTP_PROXY: http://vpn-gateway:10808
      HTTPS_PROXY: http://vpn-gateway:10808
      ALL_PROXY: http://vpn-gateway:10808
      NO_PROXY: 127.0.0.1,localhost,container-1,vpn-gateway,airmoney-vpn
      http_proxy: http://vpn-gateway:10808
      https_proxy: http://vpn-gateway:10808
      all_proxy: http://vpn-gateway:10808
      no_proxy: 127.0.0.1,localhost,container-1,vpn-gateway,airmoney-vpn
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
    expose:
      - "8000"
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import socket; s = socket.create_connection(('127.0.0.1', 8000), 3); s.close()",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - airmoney_net

networks:
  airmoney_net:
    name: airmoney_net

volumes:
  sing-box-data:
